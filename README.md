# ESA Edge Speed

> A Vue-powered speed test service for Alibaba Cloud ESA edge functions, packaged as a single JavaScript deployment file with build-time token protection.

[![Vue 3](https://img.shields.io/badge/Vue_3-42b883?style=for-the-badge&logo=vuedotjs&logoColor=white)](https://vuejs.org/)
[![Vite](https://img.shields.io/badge/Vite-646cff?style=for-the-badge&logo=vite&logoColor=white)](https://vite.dev/)
[![Edge Function](https://img.shields.io/badge/ESA_Edge_Function-ff6a00?style=for-the-badge&logo=alibabacloud&logoColor=white)](https://www.alibabacloud.com/help/zh/edge-security-acceleration/esa/user-guide/what-is-functions-and-pages/)
[![Single File JS](https://img.shields.io/badge/Single_File_JS-1f7a5c?style=for-the-badge&logo=javascript&logoColor=white)](./edge/speedtest-edge.js)
[![App Ready](https://img.shields.io/badge/App_API_Ready-2f4858?style=for-the-badge)](./APP_INTEGRATION.md)
[![Token Protected](https://img.shields.io/badge/Build_Time_Token-8a5a14?style=for-the-badge)](#token-format)

ESA Edge Speed is a browser and app-facing network speed test server for Alibaba Cloud ESA / EdgeRoutine. It provides latency, jitter, one-shot download, continuous multi-connection flow testing, and optional upload testing through one edge function.

The frontend is Vue. The backend is JavaScript. The deployable edge artifact is generated as a single file: `dist-edge/edge.js`.

## GitHub Metadata

Recommended repository description:

```text
Single-file Alibaba Cloud ESA edge speed test server with Vue UI, app APIs, streaming download, flow testing, upload testing, and build-time token protection.
```

Recommended topics:

```text
aliyun-esa, edge-function, edgeroutine, speedtest, network-testing, vue3, vite, single-file-js, app-api, token-protection
```

## Highlights

- Browser speed test UI with latency, jitter, one-shot download, flow testing, parallel streams, and optional upload.
- App-ready `/api/*` endpoints for native apps, WebView, mini programs, and monitoring probes.
- Single-file edge backend in [edge/speedtest-edge.js](./edge/speedtest-edge.js).
- Single-file ESA deploy output generated by `npm run build:edge`.
- Build-time token injection for ESA environments where runtime environment variables are unavailable.
- Streaming binary responses via `ReadableStream` to avoid large memory allocation.
- `no-store` and `no-transform` response headers to keep speed test traffic from being cached or compressed.
- CORS allowlist, request size limits, upload limits, and constant-time token comparison.

## Architecture

```text
Client browser or App
        |
        | HTTPS
        v
Alibaba Cloud ESA edge node
        |
        | single JavaScript function
        v
edge/speedtest-edge.js
        |
        +-- Vue static assets
        +-- /api/config
        +-- /api/ping
        +-- /api/download
        +-- /api/flow
        +-- /api/upload
```

## Project Layout

```text
.
├── edge/speedtest-edge.js     # ESA edge function source
├── src/                       # Vue frontend
├── tools/local-edge.mjs       # Local fetch-handler simulator
├── tools/inline-static.mjs    # Inlines Vue dist into one edge file
├── tools/privacy-scan.mjs     # Public-repo safety scan
├── tools/smoke-test.mjs       # API smoke test
├── APP_INTEGRATION.md         # App integration protocol
├── SECURITY.md                # Token and public-repo safety notes
└── dist-edge/edge.js          # Generated deploy artifact, ignored by git
```

## Quick Start

```bash
npm install
npm run dev
```

Local services:

- Vue frontend: `http://127.0.0.1:5173`
- Local edge API simulator: `http://127.0.0.1:8787`

Vite proxies `/api/*` to the local edge simulator.

## Build For ESA

```bash
npm run build:edge
```

Upload `dist-edge/edge.js` to ESA Functions / EdgeRoutine and bind the target route or domain to that function.

Important: `dist-edge/edge.js` may contain build-time access tokens. It is a deploy artifact only. Do not commit it to a public repository. This repo ignores `dist/` and `dist-edge/`.

## Token Format

ESA functions cannot read runtime environment variables in this deployment model, so access tokens are injected at build time by `tools/inline-static.mjs`.

Recommended token format:

- At least 32 characters.
- Prefer 32 random bytes encoded as base64url, usually 43 characters.
- No whitespace.
- No placeholder text such as `example`, `replace`, `secret`, `password`, or `token`.
- Never use a `VITE_` prefix for private tokens. Vite exposes `VITE_` variables to frontend code.

Generate a token:

```bash
openssl rand -base64 32 | tr '+/' '-_' | tr -d '='
```

Build with one token:

```bash
SPEEDTEST_TOKEN='<base64url-random-43-chars-or-longer>' npm run build:edge
```

Build with multiple tokens for app key rotation:

```bash
SPEEDTEST_TOKENS='<token-a>,<token-b>' npm run build:edge
```

Restrict CORS origins during build:

```bash
SPEEDTEST_TOKEN='<base64url-random-43-chars-or-longer>' \
SPEEDTEST_ALLOWED_ORIGINS='https://speed.example.com,https://app.example.com' \
npm run build:edge
```

## API

| Method | Path | Purpose | Auth |
| --- | --- | --- | --- |
| `GET` | `/api/config` | Public limits and edge metadata | No |
| `GET` | `/api/health` | Health check | No |
| `GET` | `/api/ping` | Latency and jitter sampling | Yes, when token is enabled |
| `GET` | `/api/download?bytes=67108864` | One-shot download speed test | Yes, when token is enabled |
| `GET` | `/api/flow?bytes=536870912` | Continuous flow test, client cancels by time | Yes, when token is enabled |
| `POST` | `/api/upload` | Upload speed test | Yes, when token is enabled |

App integration details are in [APP_INTEGRATION.md](./APP_INTEGRATION.md).

## Runtime Defaults

The default limits live in [edge/speedtest-edge.js](./edge/speedtest-edge.js). Do not write real tokens into source code.

```js
const DEFAULT_CONFIG = {
  allowedOrigins: ["*"],
  defaultSingleBytes: 64 * 1024 * 1024,
  maxDownloadBytes: 512 * 1024 * 1024,
  maxFlowBytes: 1024 * 1024 * 1024,
  maxUploadBytes: 64 * 1024 * 1024,
  maxParallel: 8
};
```

Production recommendations:

- Set `SPEEDTEST_TOKEN` or `SPEEDTEST_TOKENS` during build.
- Put the same token in app requests via `X-Speedtest-Token`.
- Set `SPEEDTEST_ALLOWED_ORIGINS` to your production domain and app WebView origins.
- Exclude `/api/download` and `/api/flow` from platform compression and caching rules.
- Rotate tokens by building once with both old and new tokens, shipping the app update, then rebuilding with only the new token.

## Verification

Build and generate the ESA deploy file:

```bash
npm run build:edge
```

Run the local API simulator:

```bash
npm run dev:api
```

Run API smoke tests:

```bash
npm run test:smoke
```

Run the public repository safety scan:

```bash
npm run scan:privacy
```

## ESA Notes

Alibaba Cloud documentation describes ESA functions as an edge JavaScript runtime that supports ES6 and standard Web Service Worker APIs. The examples use `addEventListener("fetch", ...)` and `new Response(...)`. The documented constraints include a 120-second function response time, a 10-second first-byte gateway wait, and a 4 MB single JavaScript file limit. This project uses streaming responses and checks the generated file size during build.

References:

- [Alibaba Cloud ESA Functions and Pages overview](https://www.alibabacloud.com/help/zh/edge-security-acceleration/esa/user-guide/what-is-functions-and-pages/)
- [EdgeRoutine JSON response example](https://www.alibabacloud.com/help/zh/edge-security-acceleration/dcdn/user-guide/return-an-output-in-json-format)

## License

MIT
